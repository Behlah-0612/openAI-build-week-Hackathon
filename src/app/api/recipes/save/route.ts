import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { recipeOptionsSchema } from "@/lib/validation";
export async function POST(request: Request) { const parsed = recipeOptionsSchema.shape.recipes.element.safeParse(await request.json()); if (!parsed.success) return NextResponse.json({ error: "Invalid recipe" }, { status: 400 }); const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); const r = parsed.data; const { error } = await supabase.from("recipes").insert({ user_id: user.id, title: r.title, ingredients_json: r.ingredients, steps_json: r.steps, time_minutes: r.time_minutes, missing_ingredients_json: r.missing_ingredients, estimated_cost: r.estimated_cost, source: "ai_generated" }); return error ? NextResponse.json({ error: error.message }, { status: 400 }) : NextResponse.json({ ok: true }); }
