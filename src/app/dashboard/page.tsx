import Link from "next/link";
import { Plus, Sparkles } from "lucide-react";
import { createClient } from "@/lib/supabase/server";
export default async function Dashboard() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); const { data: items } = user ? await supabase.from("pantry_items").select("id, name, expires_at").order("expires_at", { ascending: true }).limit(5) : { data: [] }; return <main className="p-5"><p className="text-sm font-semibold text-herb">YOUR KITCHEN</p><h1 className="mt-1 text-3xl font-bold">Good to see you 👋</h1><section className="mt-6 rounded-3xl bg-herb p-5 text-white"><Sparkles size={24}/><h2 className="mt-3 text-xl font-bold">Start with what you have.</h2><p className="mt-2 text-sm text-white/80">Add a few pantry items, and I’ll turn them into dinner ideas.</p><Link href="/pantry" className="mt-4 inline-block rounded-xl bg-white px-4 py-3 text-sm font-bold text-herb">View pantry</Link></section><section className="mt-7"><div className="flex items-center justify-between"><h2 className="text-lg font-bold">Expiring soon</h2><Link href="/pantry" className="text-sm font-semibold text-herb">See all</Link></div>{items?.length ? <ul className="mt-3 grid gap-2">{items.map(i => <li className="rounded-2xl bg-white p-4 shadow-sm" key={i.id}>{i.name}<span className="float-right text-sm text-ink/60">{i.expires_at || "No expiry"}</span></li>)}</ul> : <div className="mt-3 rounded-2xl border border-dashed border-ink/20 p-6 text-center text-sm text-ink/60">Your pantry is empty. Scan a receipt or add your first item.</div>}</section><Link href="/pantry?add=1" className="fixed bottom-24 right-5 flex h-14 w-14 items-center justify-center rounded-full bg-citrus text-white shadow-lg" aria-label="Add pantry item"><Plus /></Link></main>; }
