import Link from "next/link";
import { AuthForm } from "@/components/auth-form";
export default async function Login({ searchParams }: { searchParams: Promise<{ demo?: string }> }) { const { demo } = await searchParams; return <main className="mx-auto min-h-screen max-w-md p-6"><Link href="/" className="text-sm font-semibold text-herb">← PantryChef</Link><h1 className="mt-16 text-4xl font-bold">{demo ? "Welcome to the demo" : "Welcome back"}</h1><p className="mt-3 text-ink/70">{demo ? "Explore a stocked kitchen. Your changes are reset on every demo login." : "Your kitchen is waiting."}</p><AuthForm demo={demo === "1"}/>{!demo && <Link href="/login?demo=1" className="mt-6 block text-center text-sm font-semibold text-herb">Try the fully stocked demo instead</Link>}</main>; }
